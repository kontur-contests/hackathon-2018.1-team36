class Game {
    _width;
    _height;
    constructor(width, height) {
        this._width = width;
        this._height = height;
    }

    update() {
        // for all tank recalculate
        // bullets
    }

    createTank() {
        // let id = generator();
        // return id;
    }
}
